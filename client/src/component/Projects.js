import React from 'react'

export default function Projects({ projectsRef }) {
    return (
        <div style={{ minHeight: '100vh', background: "white" }} ref={projectsRef}>

        </div>
    )
}
